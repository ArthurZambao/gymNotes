import { RegisterPage } from "@/src/modules/register/components/content";

export const metadata = {
  title: 'Cadastre-se',
};

export default function Register() {
  return <RegisterPage />
}